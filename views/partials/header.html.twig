<nav class="navbar navbar-expand-lg navbar-dark bg-primary mb-4">
    <a class="navbar-brand" href="/">Blog</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarColor01">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item">
                <a class="nav-link py-0" href="/">Accueil <span class="sr-only">(current)</span></a>
            </li>
            <li class="nav-item">
                <a class="nav-link py-0" href="/posts">Posts <span class="sr-only">(current)</span></a>
            </li>
        </ul>
        <ul class="navbar-nav ml-auto">
            {% if app.user %}
                <li class="nav-item dropdown">
                    <a href="#" class="nav-link py-0 dropdown-toggle" data-toggle="dropdown" id="accountDropdownLink">
                        {% if app.user.picture is null %}
                        {{app.user.fullName}}
                        {% else %}

                        <img src="{{ app.user.picture }}" class="avatar avatar-mini"
                             alt="Avatar de {{app.user.fullName}}"> {{app.user.fullName}}
                    </a>
                    {% endif %}

                    <div class="dropdown-menu dropdown-menu-right dropdown-menu--animate" aria-labelledby="accountDropdownLink">

                            <a href="" class="dropdown-item">Créer une offre</a>
                            <div class="dropdown-divider"></div>

                        <a href="" class="dropdown-item">Mon compte</a>
                        <a href="}" class="dropdown-item">Modifier mon profil</a>
                        <div class="dropdown-divider"></div>
                        <a href="" class="dropdown-item">Déconnexion</a>
                    </div>
                </li>
            {% else %}
                <li class="nav-item">
                    <a href="" class="nav-link">Inscription <i class="fa fa-sign-in"></i></a>
                </li>
                <li class="nav-item">
                    <a href="" class="nav-link">Connexion <i class="fa fa-user"></i></a>
                </li>
            {% endif %}
        </ul>
    </div>
</nav>
